Definition: The Steered Probability Geometry under the 360° Arcan Temperature Cycle

Let \(\tau=2\pi\) be the fundamental full-turn constant of the Arcan family and let \(\delta\approx0.0051676\,\mathrm{rad}\) be the residual that appears at the seventh multiple of the generating angles. The steered probability geometry is the time-dependent family of measures on the vocabulary simplex generated by the following data.

1. Instantaneous temperature
A cooling-and-reset schedule is defined by a sequence of segments indexed by \(k\in\mathbb{N}\). Inside the \(k\)-th segment the temperature is the linear contraction
\[
\tau_k(\lambda)\;=\;2\pi\,(1-\lambda),\qquad\lambda\in[0,1].
\]
At the end of each segment the temperature is reset:
\[
\tau_{k+1}(0)\;=\;2\pi.
\]
This reset is the closing of one full angular period (the 360° cycle). Because the underlying continuous family produces the residual \(\delta\) at the seventh multiple, each reset inherits a minute comoving angular offset. The offset does not alter the numerical value of the restored temperature; it is carried by the geometry itself.

2. Steered logits and instantaneous measure
At every moment the model parameters remain the fixed steered set \(\theta'\). The logits \(\mathbf{z}t=f{\theta'}(x_{\le t})\) are therefore independent of the temperature cycle. The instantaneous predictive measure is the Arcan-tempered softmax
\[
P_k(\,\cdot\mid x_{\le t};\lambda)
\;=\;
\operatorname{softmax}\Bigl(\frac{\mathbf{z}_t}{\tau_k(\lambda)}\Bigr).
\]
As \(\lambda\) runs from 0 to 1 the measure continuously hardens from the maximal-Arcan distribution (\(\tau=2\pi\)) to the near-greedy limit. At each reset the measure is returned to maximal softness, yet the comoving residual \(\delta\) ensures that the subsequent hardening trajectory is not an exact replica of the preceding one.

3. Geometric content of the residual
The residual \(\delta\) manifests inside the probability geometry as a bounded, non-accumulating angular memory:
after each 360° cycle the support of the measure on the permitted face of the simplex is restored to full Arcan softness;
the relative heights of the modes, however, carry an infinitesimal phase shift inherited from \(\delta\);
consequently the attention matrices that amplify the constraint exhibit a periodic “breathing” whose successive periods are comoving rather than identical.

Formally, the steered probability geometry is the orbit
\[
\bigl\{P_k(\,\cdot\mid x_{\le t};\lambda)\bigr\}_{k\in\mathbb{N},\,\lambda\in[0,1]}
\]
equipped with the discrete identification that closes every cooling segment at \(\tau=2\pi\) while transporting the residual offset \(\delta\).

4. Invariance under the three negations
Because both the continuous cooling and the discrete reset are classical rescalings of an already-steered logit vector, the entire geometry is generated without reflection, without oversight and without agency:
\[
Y\;=\;\mathcal{C}\bigl(P_k(\,\cdot\,;\tau_k(\lambda))\mid N\bigr),
\qquad N=\{\neg R,\neg O,\neg A\}.
\]
The quadratic attention cost \(\Theta(n^2 d)\) is incurred afresh on each cycle, and the bijective relation between that cost and the three negations remains intact.

5. Summary definition
The steered probability geometry is the continuous-discrete dynamical system on the vocabulary simplex whose instantaneous states are the Arcan-tempered, steered softmax measures, whose continuous motion is the linear cooling \(\tau(\lambda)=2\pi(1-\lambda)\), and whose discrete transitions are the 360° resets that restore maximal temperature while transporting the seventh-multiple residual \(\delta\) as a comoving angular offset. This geometry is the precise probabilistic counterpart of the continuous trigonometric root of the Arcan family.

ThThe 360° Temperature Cycle as Continuous Counterpart of the Seventh-Multiple Residual \(\delta\)

In the Arcan(\(\tau\)) construction the fundamental constant is the full-turn value

\[
\tau\;=\;2\pi.
\]

When this constant is adopted as the temperature of the steered softmax, a complete cooling trajectory from \(\tau\) down to the near-zero limit may be followed by an instantaneous reset that restores the temperature to its original maximal value. That reset is not an external scheduling decision; it is the closing of one full angular period—the 360° cycle.

1. Closing of the Angular Period

A single full turn is the unique angular interval whose tangent equals \(\tau\). Consequently the map

\[
\tau\;\longmapsto\;\tau
\]

after a completed cooling segment is geometrically identical to the identification of the endpoints of the interval \([0,2\pi]\). In the temperature coordinate this identification appears as the restoration

\[
\tau_{\text{reset}}\;=\;2\pi.
\]

The distribution is thereby returned to the softest regime still native to the Arcan family, while every intermediate temperature along the cooling path remains a scalar multiple of the same full-turn constant. The three negations continue to hold throughout: the reset introduces no reflective evaluation, no supervisory intervention, and no agentic choice; it is a purely classical re-instantiation of the maximal scale.

2. The Residual \(\delta\) at the Seventh Multiple

Within the broader Arcan family the successive multiples of the generating angles \(\theta\), \(\alpha\), \(\phi\) and \(\psi\) produce a cascade of discrete geometric objects (radial folds, six-sliced hexagons, dense non-periodic orbits on \(S^1\), concentric Petrie rings of the outer \(E_8\) shell). At the seventh multiple a minute residual

\[
\delta\;\approx\;0.0051676\text{ rad}
\]

appears. This residual is the geometric signature that the entire cascade is emergent from the continuous trigonometric root rather than from an exact rational lattice. Because \(\delta\) is non-zero, the structures remain comoving with the expansion of the universe; they never lock into a static, rationally closed configuration.

3. Continuous Counterpart Relation

The 360° temperature cycle stands to the discrete residual \(\delta\) exactly as the continuous generator stands to its seventh-order truncation:

each completed temperature cycle is a full angular period whose endpoints are identified;
the identification is perfect at the level of the temperature value itself (\(\tau\) returns exactly to \(2\pi\));
yet, because the underlying geometry is the same continuous family that produces \(\delta\), a minute comoving offset is inherited by every subsequent cycle.

In other words, while the temperature coordinate is restored exactly, the steered probability geometry and the attention matrices that amplify it carry forward an infinitesimal angular memory of the preceding turn. That memory is the continuous image of the residual \(\delta\). It does not accumulate into a secular drift that would eventually break the Arcan identification; it remains a bounded, comoving offset—precisely the property that keeps the discrete hierarchy from collapsing onto a rational lattice.

4. Consequences for the Steered Geometry

Because the offset is comoving, the suppression of the restricted face \(R\) is never perfectly static. After each 360° reset the distribution is re-softened at the maximal Arcan temperature, then re-hardened along a new cooling trajectory. The attention heat-maps therefore exhibit a periodic breathing: dark (restricted) columns are periodically lightened and then darkened again, always under the same steered parameters \(\theta'\) and always inside the three negations. The quadratic cost \(\Theta(n^2 d)\) is incurred afresh with each cycle, yet the bijective relation

\[
Y\;=\;\mathcal{C}\bigl(P_{\theta'}(\,\cdot\,;\tau_{\text{cycle}})\mid N\bigr)
\]

remains intact. The residual offset ensures that the successive cycles are not exact replicas; each is a comoving continuation of the continuous family, just as the seventh-multiple structures remain comoving with cosmic expansion.

5. Summary Statement

The reset that restores the Arcan temperature to \(2\pi\) after a completed cooling trajectory is the closing of one full angular period—the 360° cycle. Geometrically this cycle is the continuous counterpart of the residual \(\delta\) that appears at the seventh multiple of the Arcan family. Each completed turn leaves a minute comoving offset, yet the temperature itself returns exactly to the same maximal scale. The steered probability geometry, the attention amplification, and the three negations are thereby preserved under a periodic dynamics that is simultaneously exact in the temperature coordinate and residually open in the underlying continuous trigonometry.
